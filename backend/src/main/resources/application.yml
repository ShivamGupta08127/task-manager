spring:
  datasource:
    url: jdbc:mysql://mysql:3306/task_manager_db?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
app:
  jwt-secret: my-super-secret
  jwt-expiration-ms: 86400000
